#include "main.h"

/**
 * mul - multiplies two integers
 * @a: functiion parameter
 * @b: function parameter
 * Return: the result of a multiplied by b
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
