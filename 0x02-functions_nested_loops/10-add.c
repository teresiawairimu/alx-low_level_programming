#include "main.h"

/**
 * add - adds two integers
 * @num1: function parameter that is used to pass a value from the calling code
 * @num2: function parameter that is used to pass a value from the calling code
 * Return: the result
 */
int add(int num1, int num2)
{
	int result;

	result = num1 + num2;
	return (result);
}
